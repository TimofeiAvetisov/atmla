{
    "version": 8,
    "configurePresets": [
        {
            "name": "ReleaseWithOutputInSource",
            "displayName": "Release build with output in source directory",
            "description": "Builds in a separate directory, but puts the final binary in source directory",
            "generator": "Ninja",
            "binaryDir": "${sourceDir}/out/build/${presetName}",
            "cacheVariables": {
                "CMAKE_BUILD_TYPE": "Release",
                "CMAKE_RUNTIME_OUTPUT_DIRECTORY": "${sourceDir}",
                "CMAKE_INSTALL_PREFIX": "${sourceDir}/out/install/${presetName}"
            }
        }
    ]
}
